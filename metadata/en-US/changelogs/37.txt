- Hybrid post-quantum SSH KEX bugfix: mlkem768x25519-sha256 now sends the KEM public key before the X25519 public key on the wire (matching OpenSSH 9.9+), and both hybrid algorithms derive the shared secret as HASH(kem_ss || x25519_ss) — SHA-256 for ML-KEM, SHA-512 for sntrup761 — before encoding it into the SSH exchange hash. 1.10.2 negotiated the algorithm but the first encrypted packet was rejected by the server.
