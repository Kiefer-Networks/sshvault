- Correctif KEX hybride post-quantique : mlkem768x25519-sha256 envoie désormais la clé publique KEM avant la clé X25519 (conforme à OpenSSH 9.9+) ; les deux algorithmes hybrides dérivent le secret partagé via HASH(kem_ss || x25519_ss) — SHA-256 pour ML-KEM, SHA-512 pour sntrup761 — avant l'encodage dans le hachage d'échange SSH. 1.10.2 négociait l'algorithme mais le premier paquet chiffré était rejeté par le serveur.
